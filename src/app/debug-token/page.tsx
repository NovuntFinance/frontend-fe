'use client';

/**
 * Debug page removed for security.
 * Token inspection must never be available in the browser UI.
 */
export default function DebugTokenPage() {
  return (
    <div className="flex min-h-screen items-center justify-center p-8">
      <p className="text-muted-foreground">
        This page has been removed for security.
      </p>
    </div>
  );
}
